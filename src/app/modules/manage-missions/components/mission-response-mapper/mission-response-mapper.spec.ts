import { MissionResponseMapper } from './mission-response-mapper';

describe('MissionResponseMapper', () => {
  it('should create an instance', () => {
    expect(new MissionResponseMapper()).toBeTruthy();
  });
});
